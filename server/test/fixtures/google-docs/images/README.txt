Takeout writes a document's inline images beside it. The importer does not walk
this directory and does not carry image files into the record; an <img> in a
document becomes a link to where the file sat in the export.
